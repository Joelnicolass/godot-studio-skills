# Agent — MpKit Example

Read `RULES.md` before editing. Authority: `PRD.md` > `FEATURES.md` > `RULES.md` > `VISUAL.md` > `RFCs/`.

Architecture: **standard Godot**. Multiplayer: 1P + listen + dedicated (MpKit). Art: placeholders. UI in the product language (`glue/demo_copy.gd`); code IDs in English.

Do not put gameplay in `addons/mp_kit`. Re-copy the addon from the parent repo with `./install.sh --addon-only example`.
